{
  "engines": {
    "node": "14.x || 16.x",
    "yarn": "3.x"
  },
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "client:build": "yarn workspace client-client build",
    "server:build": "yarn workspace server-app build",
    "build": "yarn run server:build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "resolutions": {
    "@types/react": "17.0.2",
    "@types/react-dom": "17.0.2"
  },
  "private": true
}
